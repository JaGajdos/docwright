{
  "name": "@docwright/action",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.json --noEmit && ncc build src/index.ts -o dist -m",
    "test": "vitest run"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  }
}
