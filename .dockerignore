# Source for web/action is not COPY'd into the image (only package.json for workspaces).
# Do not ignore those dirs entirely — npm ci needs their package.json in the build context.
doc
slices
.git
.gitignore
**/node_modules
**/dist
**/*.test.ts
**/*.test.js
.env
.env.*
!.env.example
